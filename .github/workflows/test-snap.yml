name: Test ROOT from Snap store

# Controls when the action will run. 
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  install-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Install dependencies
        run: |
          sudo apt-get -y install build-essential

      - name: Install ROOT
        run: |
          sudo snap install root-framework

      - name: Run ROOT
        run: |
          root --version

      - name: Test compiling
        run: |
          make

      - name: Run
        run: |
          ./analysis
          ls -la
